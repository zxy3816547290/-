#include<stdio.h>
void main()
{
	char a[100],b[100];
	while(scanf("%s%s",a,b)!=EOF)
	{
		int i,j;
		int ans=0;
		for(i=0;a[i];i++)
		{
			for(j=0;b[j];j++)
			{
				ans+=(a[i]-'0')*(b[j]-'0');
			}
		}
		printf("%d",ans);
	}
}
